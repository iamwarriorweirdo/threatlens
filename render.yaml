services:
  - type: web
    name: threatlens
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-3.0-flash
      - key: NODE_ENV
        value: production
